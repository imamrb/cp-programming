#include<bits/stdc++.h>
using namespace std;

int main()
{
    vector<pair<int, int> > v;
    int n, i, j;

    cin>>n;
    for(i=0; i<n; i++){
        scanf("%d", &m)
        f[m]++;
        v.push_back(make_pair(m, 1));
    }
    for(i=1; i<2*n; i++){
        for(j=0; j<i; j++){
            if(v[j].second==i && f[v[j].first]>1){

            }
        }
    }
}
